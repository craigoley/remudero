# Build output -- not a second source of truth. web-dist/ is a byte-for-byte copy of
# apps/dashboard (scripts/sync-web-build.mjs); regenerate it, never hand-edit it.
/web-dist

# Rust build artifacts and Tauri's generated capability schemas (regenerated by `cargo`/`tauri`).
/src-tauri/target
/src-tauri/gen

node_modules
