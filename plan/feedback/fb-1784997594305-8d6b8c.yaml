id: fb-1784997594305-8d6b8c
ts: 2026-07-25T16:39:54.305Z
raw: 'SWEEP LOOPS FOREVER ON A REVIEW-FAILING PR IT CANNOT FIX-DISPATCH (root
  cause, evidence PR #779 / 2026-07-25). The daemon sweep disposed a
  review-failing PR as "required checks red — ci-log fix, strike 1/2" NINE times
  over ~18 min (state/ledger.ndjson: 9x sweep.disposed T-MOUNTPROBE "strike
  1/2") while dispatching ZERO actual fixes (0 fix.dispatch / run.start /
  fix.done) — an unadvanceable strike that re-loops every poll, never
  escalating, never merging, burning a sweep cycle each ~2 min. TWO compounding
  defects: (1) the PR is HAND-OPENED (branch feat/mount-routing-probe-docs) with
  a trailer Remudero-Task: T-MOUNTPROBE naming a task that is NOT in the plan,
  so the fix rung — which targets a run-<taskId>-* worker branch (sweep.ts
  ~1343) — has no branch/task to dispatch against; the "strike 1/2" disposition
  is therefore a DEAD classification (chosen every poll, dispatch always a
  no-op, so the strike counter re-derives 1/2 and never exhausts to escalate).
  (2) a SEMANTIC remudero-review=failure (unmet acceptance / non-executable
  proofs) is misrouted to the ci-log fix rung, which reads CI build-log tails —
  the wrong evidence for a review-proof-dialect failure. FIX DIRECTION:
  deriveDisposition/routeFix must detect a PR it CANNOT fix-dispatch (no
  resolvable in-plan task AND/OR no run-<taskId> worker branch) and STAND DOWN —
  escalate to a human / mark needs-attention EXACTLY ONCE — rather than
  re-selecting a fix disposition whose dispatch is a guaranteed no-op. An
  N-times-repeated "strike X/cap" with zero actual dispatches is a silent stall;
  the ladder must treat disposed-fixable-but-dispatch-impossible as an escalate
  route, not a re-loop. VERIFY the disposition + fix-routing in src/lib/sweep.ts
  from source. SIBLING of W1-T176/W1-T186 (impoverished-observed-state →
  needless loop) and the review-status-sha-pinned-stall class. NOTE: the
  proximate trigger — non-executable acceptance proofs on a hand PR — is
  separately a dead-proof-floor instance (the numbered/prose Proof: block was
  rewritten to the executable grep: dialect to merge #779).'
attachments: []
origin: cli
status: rejected
proposal_pr: null
summary: null
