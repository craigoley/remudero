- id: W1-T3407
  title: "path-scope the gate config so the fleet cannot edit the gates that judge it, without a human on every merge"
  repo: remudero
  depends_on: []
  type: implement
  verify: auto
  risk: high
  status: blocked
  retirement: withdrawn
  attempts: 0
  files:
    - CODEOWNERS
    - test/codeowners-is-path-scoped.test.ts
  origin: "operator-session#gate-integrity-2026-09-11"
  rationale: |
    OPERATOR RULING 2026-09-11: "I'm good with the gate-integrity cluster." This is W1-T204's
    approved half — the other three (W1-T2914 reviewer identity, W1-T990 fleet identity,
    W1-T3103 two-phase worker) were explicitly backlogged the same day.

    THE DEFECT. The fleet merges its own PRs, and nothing stops a worker editing the very files
    that define the gates — .github/workflows/ci.yml, ci-gate.yml, the required-checks list — in
    the same PR those gates are supposed to judge. The obvious remedy, "require a human review",
    was REFUSED by the operator in W1-T204's own filing: it would put him on every autonomous
    merge and end the autonomy.

    MEASURED 2026-09-11, and this is why the narrow remedy is available:

      branches/main/protection: required_pull_request_reviews = null   (no review required at all)
      CODEOWNERS:               `*  @craigoley`                        (owns EVERY path)
      rulesets:                 none

    So CODEOWNERS today is DECORATIVE — it enforces nothing, because no review is required. And
    turning on `require_code_owner_reviews` as things stand would demand his review on every PR,
    which is exactly the refused option.

    THE DESIGN. Narrow CODEOWNERS from `*` to the gate-config paths only, THEN enable
    `require_code_owner_reviews`. A code-owner requirement fires only when an owned path changes,
    so an ordinary PR keeps merging autonomously and a PR touching the gates waits for him. That
    is path-scoped protection using the one native GitHub mechanism that is path-aware: branch
    protection and rulesets can RESTRICT file paths, but neither can require review conditionally
    on a path — CODEOWNERS is the only lever that does.

    ORDER MATTERS AND IS NOT OPTIONAL. Narrowing CODEOWNERS while reviews stay off is harmless
    (it enforces nothing either way). Enabling the requirement while CODEOWNERS still reads `*`
    would block every PR the fleet opens. The repo-side change must land first.
  acceptance:
    - claim: "CODEOWNERS names the gate-config paths rather than every path, so a code-owner requirement would be path-scoped"
      proof: "unit test: test/codeowners-is-path-scoped.test.ts"
  falsifier: |
    CLOSE THIS TASK if `require_code_owner_reviews` is already enabled on main AND CODEOWNERS no
    longer reads `*`. Re-derive both with one read each:
    `gh api repos/craigoley/remudero/branches/main/protection --jq .required_pull_request_reviews`
    and `cat CODEOWNERS`. If protection shows a code-owner requirement and CODEOWNERS is scoped,
    the mechanism is in place and this shard is withdrawn.
  notes: |
    SCOPE FENCE: the repo-side half (CODEOWNERS) is the only part a worker may land. The
    branch-protection flip is an operator action on GitHub — outward-facing, affects every open
    PR, and is deliberately left as the `verify: human` half of this record.

  note: |
    OPERATOR RULING 2026-09-22 — FLIP IT. BUILD THIS. The gate-integrity cluster's approved half
    was approved on 2026-09-11 and has sat parked since; the operator re-affirms it.

    WHY THIS ONE IS SAFE TO UNPARK WHEN ITS THREE SIBLINGS WERE BACKLOGGED. The refused option in
    W1-T204 was "require a human review", because it would put the operator on every autonomous
    merge and end the autonomy. THIS IS THE OPPOSITE SHAPE. A code-owner requirement fires ONLY
    when an owned path changes. Narrowing CODEOWNERS from `*` to the gate-config paths means an
    ordinary PR keeps merging unattended and only a PR that edits the gates waits for a human —
    which is the one case where waiting is the point. Autonomy is preserved on every path that
    matters and removed on exactly the path the fleet must not be judge of.

    THE ORDER IS LOAD-BEARING AND MUST NOT BE INVERTED. Narrow CODEOWNERS FIRST, then enable
    `require_code_owner_reviews`. Enabling it while CODEOWNERS still reads `*  @craigoley` demands
    the operator's review on EVERY pull request — the refused option, arrived at by accident. A
    build that flips the protection before the ownership file lands has shipped the thing this
    task exists to avoid.

    PROVE THE NARROWING DISCRIMINATES BEFORE THE PROTECTION GOES ON. `test/codeowners-is-path-scoped.test.ts`
    must fail if CODEOWNERS matches an ordinary source path and pass only when it matches the gate
    config paths — an assertion that a path IS owned is not enough, because `*` satisfies it. The
    falsifier is a path that must be UNOWNED.

    THE PROTECTION FLIP IS THE OPERATOR'S STEP AND IS DELIBERATELY NOT AN ACCEPTANCE CRITERION.
    The original criterion 2 declared a `demonstration:` proof, which `proof-dialect` refuses on a
    verify:auto task and `headless-fitness` refuses for naming an operator action — both correctly,
    because no harness can enable `require_code_owner_reviews` for itself. THE TASK THEREFORE SHIPS
    THE HALF A MACHINE CAN PROVE: CODEOWNERS narrowed, with a test whose falsifier is an UNOWNED
    ordinary source path (a bare `*` passes "is owned" and fails that one). The operator enables
    `require_code_owner_reviews` on `main` AFTER this merges — that order is the ruling above — and
    records the before/after protection read at that time.

    OPERATOR RULING 2026-09-22 (SECOND) - WITHDRAWN. THIS REVERSES THE 2026-09-11 APPROVAL AND THE
    EARLIER 2026-09-22 'FLIP IT' NOTE ABOVE, AND SAYS WHY. The operator's direction is automated flow
    without added human checks: "I don't want slow downs and more checks." A path-scoped
    require_code_owner_reviews is exactly such a check - every gate-config PR would wait for a human.
    NO AUTOMATED GUARD IS LOST BY WITHDRAWING IT. src/lib/risk-score.ts already lists
    `.github/workflows/` and `CODEOWNERS` in CRITICAL_PATH_PATTERNS, whose own comment says blast
    radius alone makes the WHOLE diff critical and never auto-chosen. So a fleet PR that edits the
    gates that judge it is already caught - by the automated scorer, not by a review requirement.
    DO NOT enable require_code_owner_reviews and do not narrow CODEOWNERS on this record's account.
    ACCEPTANCE TRIMMED WITH THE WITHDRAWAL: the two criteria added earlier on 2026-09-22 named
    unit tests (an unowned ordinary source path, every gate config path owned) that were forward
    references to a build that will now never happen. The changed-task lint pass correctly refuses
    a title that resolves to no test, so they are removed here rather than left as dead proofs.
