- id: W1-T3172
  title: "THE RULE-25 SPLIT WORKER HAS NO SWEEP ROUTE — review.posted records instrument_entangled and both path sets, but the board projection drops them, classifies the empty-unmet failure as blocked-ambiguous, and never reaches W1-T2436's prerequisite builder"
  repo: remudero
  depends_on: [W1-T2436]
  type: implement
  verify: auto
  principles: {tdd: strict}
  risk: high
  band_meaning: span
  budget_usd: 30.00
  files:
    - src/lib/sweep.ts
    - src/run-task.ts
    - test/entanglement-split-sweep-reachability.test.ts
  origin: "fleet-incident#pr-4559-unreachable-rule25-split-2026-09-08"
  plan_refs: ["W1-T297", "W1-T527", "W1-T923", "W1-T2436", "W1-T3029"]
  status: queued
  attempts: 0
  rationale: |
    OBSERVED ON PR #4559 AT 2026-09-08T13:23:28Z. Its authoritative `review.posted`
    row records `state: failure`, `failure_class: instrument_entangled`, an empty
    `unmet_criteria` array, and both exact path sets under
    `decision_verdict.instrumentEntanglementPaths`. The review summary correctly says to land the
    instrument change in its own PR and then rebase. At 13:31:00Z and 13:35:12Z the sweep instead
    disposed the same head as `blocked-ambiguous` with "no actionable unmet criteria
    (contradictory)" and dispatched no prerequisite worker.

    THE FULL LOSS CHAIN IS IN CURRENT MAIN. `reviewLedgerReasons` adds a structural fallback only
    for `changesetContradictions`, so a pure instrument-entanglement verdict ledgers `reasons: []`.
    `actionableGateFailuresFromLedger` therefore returns none, and the disposition table's normal
    review-fix row cannot match. The final failure row calls the verdict contradictory. Even if
    the board were routed manually, `buildFixRungDispatchArgs` reconstructs only criteria and
    summary: it drops both `instrumentEntangled` and `instrumentEntanglementPaths`. W1-T2436's
    special arm checks that boolean before spending a strike, so the prerequisite builder remains
    unreachable and an ordinary worker would be sent an in-place repair it cannot honestly make.

    THIS IS NOT A REQUEST TO WEAKEN RULE 25. W1-T527 correctly classifies gate-conflict as a
    condition an in-place patch cannot satisfy. W1-T2436 later supplied a different effect: open a
    separate prerequisite PR, wait for its own CI, park the original, and rebase only after the
    prerequisite merges. The missing work is the typed connection from the authoritative review
    record, through the board disposition, into that existing arm. W1-T3029's rule-15 refusal must remain blocked-ambiguous
    because no equivalent sanctioned worker exists for editing task
    criteria; the two structural failures must not be generalized together.
  design: |
    Recover one head-and-input-bound structural review record from the same `review.posted` ledger
    scan that already builds the board. Match the current task key, exact `head_sha`, and exact `review_input_digest`;
    last matching record wins, and a success or another class clears the
    entanglement projection. Accept only `failure_class === instrument_entangled` with two
    non-empty arrays of bounded strings under the already-recorded
    `decision_verdict.instrumentEntanglementPaths`. Missing, malformed or stale evidence is no
    authority to launch a split and preserves today's blocked-ambiguous result.

    Add typed optional fields to `OpenPrView` for the structural failure class and the two path
    sets. Add one disposition row after strike exhaustion but before ordinary review-fix routing:
    exact structured entanglement with usable path evidence is `blocked-fixable`, with a reason
    that names W1-T2436's prerequisite path and explicitly says zero ordinary strikes. Do not turn
    the summary text marker or generic `reasons` array into dispatch authority.

    Thread the same structured fields through `buildFixRungDispatchArgs` so its cold
    `initialReview` has `instrumentEntangled: true` and the exact path sets. Then the existing
    W1-T2436 arm must be the first effect: it dispatches the prerequisite worker before
    `strikes++`, uses `fix.prerequisite_opened` as its idempotency memory, waits for green, and
    parks/rebases through the existing shared helper. Do not reimplement any of those effects in
    sweep, spend an ordinary fix strike, merge a prerequisite, or infer paths from prose.

    The test must drive the full cold path: a ledger row shaped exactly like #4559 becomes a board
    view, derives `blocked-fixable`, builds a cold rung carrying both path sets, and reaches the
    prerequisite spawn with zero ordinary fix dispatches. Negative controls cover stale head,
    changed body digest, malformed/missing path data, rule 15, ordinary unmet criteria, and strike
    exhaustion. Deleting any one of the producer, disposition or cold-rung threading links must
    restore the observed blocked-ambiguous/no-spawn failure.
  acceptance:
    - claim: "an exact-head exact-input instrument_entangled ledger row with empty unmet criteria derives blocked-fixable rather than contradictory blocked-ambiguous"
      proof: "unit test: test/entanglement-split-sweep-reachability.test.ts"
    - claim: "the cold fix-rung reconstruction carries instrumentEntangled and both exact path sets into W1-T2436's existing prerequisite-worker arm"
      proof: "unit test: test/entanglement-split-sweep-reachability.test.ts"
    - claim: "the first effect for that cold route is a prerequisite spawn with zero ordinary fix strikes, not an in-place gate-fix worker"
      proof: "unit test: test/entanglement-split-sweep-reachability.test.ts"
    - claim: "a stale head, changed input digest, missing or malformed path set, or non-entanglement failure cannot launch the split worker"
      proof: "unit test: test/entanglement-split-sweep-reachability.test.ts"
    - claim: "Standing rule 15 and a genuinely empty unexplained failure remain blocked-ambiguous, while ordinary unmet criteria retain their existing fix route"
      proof: "unit test: test/entanglement-split-sweep-reachability.test.ts"
    - claim: "strike exhaustion still wins before the split route and no prerequisite is merged or rebased before its independently green merge state"
      proof: "unit test: test/entanglement-split-sweep-reachability.test.ts"
    - claim: "removing the producer, disposition, or cold-rung threading link reproduces PR #4559's blocked-ambiguous no-spawn state"
      proof: "unit test: test/entanglement-split-sweep-reachability.test.ts"
  falsifier: |
    Close without implementation if a current-main board view built from #4559's exact ledger row
    already derives `blocked-fixable` and its first cold fix effect is
    `renderPrerequisitePrPrompt` with both recorded path sets. Current main at db770194 drops the
    structured fields and the live sweep twice emitted blocked-ambiguous, directly falsifying that
    alternative.

    Prove each link load-bearing independently. Neuter the ledger projection while leaving routing
    and the rung intact: the fixture must fail closed. Neuter only the disposition row: it must
    return to contradictory blocked-ambiguous. Neuter only cold-rung threading: no prerequisite
    spawn may be observed. A test that stays green after any of those mutations proves only a
    helper, not the production chain.
  note: |
    ID PROVENANCE: `rmd next-task-id --reserve` skipped W1-T3171 because an open PR already held it
    and atomically reserved `refs/rmd-id/W1-T3172`; the reserved id is authoritative. ONE CONCERN:
    make W1-T2436 reachable from the cold sweep using evidence the reviewer already records.
    Rule 25 checked before filing: the implementation changes product source plus its own test and
    no measurement-instrument path. W1-T3072's author-time prevention is a separate upstream
    question; it does not clear already-open entanglement and is not folded into this repair.

    PROOF AMENDMENT 2026-09-08. Source PR #4666 passed its 7-test focused suite, 29 related tests,
    a 274-test regression set and fast preflight, but all seven original `grep:` proofs were
    comments inside the newly added test file. The reviewer's base comparison deliberately copies
    an added test into the base checkout; each comment therefore matched both trees and graded
    `executed_stale`, leaving 0/7 executable evidence. The replacement runs the focused test file:
    it passes on the source head and fails against current main, so it discriminates implementation
    from absence. Claims, scope, code, acceptance count and fail-closed behavior are unchanged.
