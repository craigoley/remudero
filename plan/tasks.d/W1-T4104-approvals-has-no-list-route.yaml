- id: W1-T4104
  title: "THE CONSOLE'S APPROVALS PAGE ASKS FOR /v1/operator-agent/consequences AND CORE ONLY SERVES THE PREFLIGHT — W1-T3894 shipped /consequence/preflight and no list, so Approvals can never show a pending consequence; serve a bounded, read-only list of pending consequence approvals"
  repo: remudero
  depends_on: []
  priority: 3
  type: implement
  verify: auto
  principles: {tdd: strict}
  budget_usd: 30.00
  risk: medium
  origin: "operator-report#console-not-working-2026-09-23"
  plan_refs: ["W1-T3894", "W1-T4103"]
  files:
    - src/lib/operator-agent.ts
  status: queued
  attempts: 0
  rationale: |
    READ 2026-09-23: the console's /approvals route reads GET /v1/operator-agent/consequences.
    On origin/main 3d3c61590 the only consequence route registered in src/ is
    /v1/operator-agent/consequence/preflight (src/lib/operator-agent.ts), which W1-T3894 shipped.
    The page's read therefore 404s and Approvals renders unavailable on every visit. A task that
    already merged cannot be re-opened, and no other task covers the list, so this files it.
  acceptance:
    - claim: "a bounded list of pending consequence approvals is served read-only"
      proof: "unit test: pending consequence approvals are listed read-only and bounded"
    - claim: "an empty queue answers an empty list with its read time, never a 404"
      proof: "unit test: an empty approval queue answers an empty list with its read time"
    - claim: "the route refuses a write verb by name"
      proof: "unit test: the consequence list refuses a write verb by name"
