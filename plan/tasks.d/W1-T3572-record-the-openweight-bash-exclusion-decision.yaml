- id: W1-T3572
  title: "RECORD THE OPENWEIGHT BASH EXCLUSION DECISION — the adapter intentionally implements no Bash function, so establish whether every shell-capable lane remains premium or a separately bounded execution surface is warranted"
  repo: remudero
  depends_on: [W1-T3546]
  type: recon
  verify: human
  principles: {tdd: strict}
  risk: high
  band_meaning: blast-radius
  budget_usd: 0.00
  files:
    - docs/recon/openweight-bash-capability-decision.md
  origin: "operator-session#open-weight-resequence-2026-09-14"
  plan_refs: ["W1-T3545", "W1-T3569", "W1-T3570"]
  status: queued
  attempts: 0
  note: |
    reservation hand-off: main -> chore/file-openweight-bash-decision-1789389121907

    OBSERVED at origin/main 1aad6122: OPENWEIGHT_FUNCTIONS declares Read, Write,
    Edit, Grep and Glob only (src/lib/worker-provider.ts:1778); the worker surface
    defaults to unrestricted when its deny list is omitted (src/lib/worker.ts:852-857,
    1846-1848),
    and FIX_WORKER_TOOLS is an explicit different fence. This record prevents later
    routing from treating a mount's cheap model/effort as proof it can safely execute
    a shell-capable lane. It does not add Bash, alter any tool allowlist, or route a lane.
  rationale: |
    Supplying an unimplemented tool would either make the adapter refuse correctly or
    tempt an implementation to grant filesystem/process authority in a paid remote
    model loop. Neither is a routine routing edit. The immediate inbox-draft trial has
    no Bash surface, so this decision can stay independent of its measurement.
  design: |
    Inventory every production spawn that can expose Bash, classify whether the tool is
    essential versus historical default, and record one of two explicit decisions:
    preserve the premium provider boundary, or propose a separately reviewed bounded
    execution design with containment, path, timeout, concurrency, credential and cash
    proofs. Treat missing evidence as preserve. The report must cite exact call sites,
    include a positive control that the inventory sees a known Bash-capable lane, and
    state that no route becomes eligible merely because it uses a low-effort mount.
  acceptance:
    - claim: "the report maps each Bash-capable production spawn to its explicit or default tool boundary and includes a positive-control lane"
      proof: "grep: Positive control: in docs/recon/openweight-bash-capability-decision.md"
    - claim: "the report records one preserve or separately-design decision without adding Bash to the adapter"
      proof: "grep: Decision: in docs/recon/openweight-bash-capability-decision.md"
  falsifier: |
    An inventory that finds no known Bash-capable positive control cannot support an exclusion
    claim. A report that silently introduces an adapter function, changes a lane provider, or
    treats an absent tool as harmless has exceeded this recon's scope and must be split.
