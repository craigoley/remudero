- id: W1-T4201
  title: "A RUN WHOSE FINAL REPORT IS ALREADY RECORDED CAN STILL HOLD ITS INFLIGHT LOCK THROUGH A LINGERING CHILD, AND NOTHING RELEASES IT OR STOPS A RE-DISPATCH — the reclaim rungs handle dead pids only: release the exact lock of a run with a terminal receipt and never re-dispatch it"
  repo: remudero
  depends_on: []
  type: implement
  verify: auto
  principles: {tdd: strict}
  budget_usd: 30.00
  risk: high
  band_meaning: span
  origin: "feedback#fb-1789328775534-a9f155"
  plan_refs: []
  files: [src/run-task.ts, src/lib/inflight-lock.ts, test/a-reported-run-releases-its-lock.test.ts]
  status: queued
  attempts: 0
  rationale: |
    READ 2026-09-23 AT origin/main b5c2804 during a backlog triage of feedback entries still at `status: new` that no plan record cited. File:line citations are from that read and are approximate (~); re-verify at your head before relying on them (binding rule: distrust the prompt over the installed code).

    OBSERVED: `reclaim_dead_inflight_locks` (#5680) and W1-T3629's orphan reap handle a dead holder or an orphaned
    process whose run is gone; no code path handles a LIVE holder whose final REPORT already exists.
    INFERRED, not traced to a line: how the worker lingers after its report (the report cites a socket-only child).
    Recorded 2026-09-13: W1-T3167's final REPORT was in its run tail while its lock stayed held and a recycle deadlocked.

    risk: high is Rule 19's span, not blast radius.

    VALUE (triage estimate): medium.
  design: |
    (i) First reproduce the lingering holder with a fixture; if it cannot be reproduced, report that instead of
        building a guess. (ii) A run whose terminal receipt is recorded releases exactly its own lock, and the dispatcher
        never re-dispatches a run id with a terminal receipt.
  acceptance:
    - claim: "a run with a recorded final report releases its own lock"
      proof: 'grep: test("W1-T4201: a run with a recorded final report releases its own lock" in test/a-reported-run-releases-its-lock.test.ts'
    - claim: "a run with a terminal receipt is never re-dispatched"
      proof: 'grep: test("W1-T4201: a run with a terminal receipt is never re-dispatched" in test/a-reported-run-releases-its-lock.test.ts'
