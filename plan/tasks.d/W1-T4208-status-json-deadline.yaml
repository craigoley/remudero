- id: W1-T4208
  title: "`rmd status --json` HAS NO OVERALL DEADLINE — each GitHub read is bounded at 60 s but several run in sequence, and the operator's health probe was measured taking 146 s: return a machine-readable degraded result per external read instead of hanging"
  repo: remudero
  depends_on: []
  type: implement
  verify: auto
  principles: {tdd: strict}
  budget_usd: 30.00
  risk: low
  origin: "feedback#fb-1789929456932-8dfc2f"
  plan_refs: []
  files: [src/lib/report-commands.ts, test/status-json-deadline.test.ts]
  status: queued
  attempts: 0
  rationale: |
    READ 2026-09-23 AT origin/main b5c2804 during a backlog triage of feedback entries still at `status: new` that no plan record cited. File:line citations are from that read and are approximate (~); re-verify at your head before relying on them (binding rule: distrust the prompt over the installed code).

    OBSERVED: `statusCommand` (src/lib/report-commands.ts ~726) has no overall deadline and builds the board
    synchronously; each GitHub call is bounded only by GH_CALL_TIMEOUT_MS = 60 s (src/lib/github-transport.ts ~11).
    INFERRED: the 146 s observation is several of those bounds in sequence.

    VALUE (triage estimate): medium.
  design: |
    (i) The JSON output completes within a declared overall budget. (ii) Each external read that did not finish is
        reported as `unavailable` with its reason, never as an empty or zero value.
  acceptance:
    - claim: "status json returns within its deadline when a read hangs"
      proof: 'grep: test("W1-T4208: status json returns within its deadline when a read hangs" in test/status-json-deadline.test.ts'
    - claim: "an unfinished read is reported as unavailable, not empty"
      proof: 'grep: test("W1-T4208: an unfinished read is reported as unavailable, not empty" in test/status-json-deadline.test.ts'
