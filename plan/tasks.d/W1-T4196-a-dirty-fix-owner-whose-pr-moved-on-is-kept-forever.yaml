- id: W1-T4196
  title: "A DIRTY ABANDONED FIX WORKTREE WHOSE PR HEAD HAS SINCE MOVED IS KEPT FOREVER — W1-T3822's recovery returns keep/remote_head_changed for exactly the reported case (old dispatch, new head, dirty tree), so the PR's only repair rung stays stood down: preserve the tree to a recovery ref and re-arm one fix"
  repo: remudero
  depends_on: []
  type: implement
  verify: auto
  principles: {tdd: strict}
  budget_usd: 30.00
  risk: high
  band_meaning: span
  origin: "feedback#fb-1789866011295-8ea8f7"
  plan_refs: []
  files: [src/run-task.ts, test/fix-branch-checkout-serialization.test.ts]
  status: queued
  attempts: 0
  rationale: |
    READ 2026-09-23 AT origin/main b5c2804 during a backlog triage of feedback entries still at `status: new` that no plan record cited. File:line citations are from that read and are approximate (~); re-verify at your head before relying on them (binding rule: distrust the prompt over the installed code).

    OBSERVED: `decideRegisteredFixOwnerRecovery` (src/run-task.ts ~34765) returns keep with reason
    remote_head_changed when the remote head moved (~34776) and keeps untracked-dirty trees (~34773). W1-T3822 (#6208)
    added dirty-owner recovery, but only for an unmoved head.
    OBSERVED: `fixRungStalledWithoutNewHead` (src/lib/sweep.ts ~7943) has no time bound for a dispatch that never wrote a
    terminal row. The report (console PR #1364) is the old-dispatch, new-head, dirty-worktree case, so it never recovers.

    risk: high is Rule 19's span (an existing suite, not this task's own falsifier), not blast radius.

    VALUE (triage estimate): medium.
  design: |
    (i) When the owner is dead and the remote head moved, the dirty tree is saved to a recovery ref (the kind W1-T3822
        already writes) and the worktree is released, so the next pass may dispatch one fix against the new head.
        (ii) A live owner is never touched. (iii) The recovery ref is named on the ledger row.
  acceptance:
    - claim: "a dead dirty owner whose head moved is preserved and released"
      proof: 'grep: test("W1-T4196: a dead dirty owner whose head moved is preserved and released" in test/fix-branch-checkout-serialization.test.ts'
    - claim: "a live owner is kept whatever its head"
      proof: 'grep: test("W1-T4196: a live owner is kept whatever its head" in test/fix-branch-checkout-serialization.test.ts'
