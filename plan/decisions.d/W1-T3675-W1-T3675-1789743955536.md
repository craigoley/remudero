## 2026-09-18T15:13:07.457Z — W1-T3675 (W1-T3675-1789743955536)
- Options: : implement rename-resilience for grep proofs by treating a missing target path (often caused by a rename) as no-match rather than a hard failure. Concretely, adjust the grep run path so that a status code of 2 from the grep invocation yields a no-match verdict instead of throwing. This allows a PR body whose grep: proof named a path that has since been renamed to continue to be reviewable without requiring a human update in CI. This is the minimal, low-risk change that aligns with the issue: the path is read only at review, not read during the change itself, and a rename should not permanently invalidate the proof. | ALTERNATIVE: implement a full rename-mapping mechanism that scans the diff for renames and rewrites the grep: target path to the new name before spawning the grep. This would require adding a reliable diff-scanning path, a mapping lookup, and updating every call-site that constructs grep argv to use the mapped target. It is more invasive and riskier, as it touches multiple layers (diff parsing, argv construction, and possibly tests that rely on exact path shapes). If the rename-detection route is needed, it should be done in a separate plan-only PR to avoid destabilising the current change.
- Chosen (RECOMMENDED, auto): : implement rename-resilience for grep proofs by treating a missing target path (often caused by a rename) as no-match rather than a hard failure. Concretely, adjust the grep run path so that a status code of 2 from the grep invocation yields a no-match verdict instead of throwing. This allows a PR body whose grep: proof named a path that has since been renamed to continue to be reviewable without requiring a human update in CI. This is the minimal, low-risk change that aligns with the issue: the path is read only at review, not read during the change itself, and a rename should not permanently invalidate the proof.
- Risk: medium (medium-risk signal in the decision text)
- Rollback: revert the PR.
