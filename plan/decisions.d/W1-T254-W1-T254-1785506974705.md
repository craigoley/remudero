## 2026-07-31T14:16:50.890Z — W1-T254 (W1-T254-1785506974705)
- Options: Context: W1-T254 ("reliable sweep post-review — light-sweep ticker + outcome-keyed dedup + throw containment") is already fully shipped. Verification performed in this worktree: | git merge-base --is-ancestor 15a2168 HEAD → YES, ancestor. Commit 15a2168 is fix(sweep): reliable post-review sweep - light-sweep ticker, outcome dedup (W1-T254) (#720), already merged into the branch this worktree was cut from. | gh pr view 720 → merged PR body ends with the exact trailer Remudero-Task: W1-T254 and its "Acceptance criteria" section maps 1:1, in the same order, onto all four criteria in plan/tasks.yaml (outcome-keyed dedup / throw containment / light-sweep ticker / attempt ledgering + dry-run tag). | Live code matches the design exactly: daemon.ts (DaemonDeps.sweepLight, ticker around runOne), sweep.ts (SweepDeps.actionable, outcome-keyed priorActionsFromLedger, per-PR try/catch), run-task.ts (buildSweepLightHook, sweep.post_review.attempt/done/failed at line 8227, actionable: (d) => d === "post-review" at 8633). | grep -n "sweep.post_review.attempt" src/run-task.ts → present (criterion 4's proof). | Test names required by the acceptance criteria exist verbatim: test/sweep.test.ts:1873 ("post-review dedup is outcome-keyed…"), test/sweep.test.ts:2101 ("a throwing action does not abort the pass…"), test/daemon.test.ts:1883 ("the light sweep runs while runOne is in flight…"). | After npm ci (had to run with sandbox disabled — the default npm cache dir under the worker-home path threw EPERM/EBADENGINE writing outside the sandbox's allowed write set; a plain retry outside the sandbox succeeded cleanly), node --test over test/sweep.test.ts + test/daemon.test.ts → 216 + 86 tests, 0 failures. | A. Close as already-satisfied — no functional code change. Land a DECISIONS.md entry (matching the W1-T201/#993 precedent format) that cites PR #720/commit 15a2168 and walks each of the four acceptance criteria to its proof (file:line + test name/grep), so the record is auditable without re-deriving this investigation. Open a chore(plan) PR carrying only that entry. — fully reversible (revert removes only the DECISIONS.md entry; no runtime file touched), and it's the exact precedent already established for this situation. | B. Proceed to re-diff src/lib/daemon.ts, src/lib/sweep.ts, src/run-task.ts, and the two test files as if unstarted. Rejected as primary: the target state already exists identically on this branch: a "reimplementation" is either a no-op diff (nothing to commit) or a hand-authored variant that risks diverging from the tested, merged design for a task marked risk: high explicitly because it touches concurrent daemon/sweep code — not reversible in the same low-cost way if it introduces a regression in an area already proven correct.
- Chosen (RECOMMENDED, auto): A. Close as already-satisfied — no functional code change. Land a DECISIONS.md entry (matching the W1-T201/#993 precedent format) that cites PR #720/commit 15a2168 and walks each of the four acceptance criteria to its proof (file:line + test name/grep), so the record is auditable without re-deriving this investigation. Open a chore(plan) PR carrying only that entry. — fully reversible (revert removes only the DECISIONS.md entry; no runtime file touched), and it's the exact precedent already established for this situation.
- Risk: high (explicit reversibility caveat in the decision text)
- Rollback: revert the PR.
