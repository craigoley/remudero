## 2026-09-18T19:00:57.446Z — W1-T3760 (W1-T3760-1789757767032)
- Options: Rationale: The prompt notes a CadenceMarker writer (last-seen) that indicates the cadence writer is not running. If last-seen lags behind, the cadence should fail rather than silently appear healthy. This is the smallest, safest single-change approach that directly encodes the “stale rung” condition into the cadence’s own decision flow, without introducing a separate monitor process. | What to implement: | Add a lightweight last-seen record file alongside cadence marker (for example state/last-measurement-cadence-seen.json) produced by the cadence writer whenever it fires. | Extend read path: | read a new file (last-measurement-cadence-seen.json) to obtain a timestamp. | if a timestamp exists and age > STALE Cadence Window (e.g., configurable, default 5 minutes), treat as a hard refused rung: return a MeasurementCadenceDecision { fire: false, reason: "cadence stale: cadence writer appears down (lastSeen: <ISO>)" }. | Wire this into decideMeasurementCadence or the top-level cadence check so that stale-last-seen yields a refused cadence with a clear reason. | If you choose to keep the existing marker logic intact, ensure the new last-seen check short-circuits further cadence work when stale. | Tests: add a minimal test that simulates a stale last-seen timestamp and asserts that the cadence returns fire: false with the stale reason, while still preserving existing behavior when last-seen is recent or absent. | Branch name hint: run-W1-T3760-<epochMs>-cadence-last-seen | Pros: Minimal surface area, failure mode explicit, preserves existing flow. Works with a single file addition and a small conditional path. | Cons: Introduces an additional file path and a small config constant (STALE window) to manage. | Rationale: Keeps cadence logic completely separate from the cadence writer’s state; could be implemented as a separate health-check or as a separate cadence rung with its own plan. | A new module or rung that reads the cadence writer’s last-seen (via the same last-seen file or a configured source) and exposes a decision line that, if stale, refuses the cadence and optionally triggers an operator alert or a follow-up action. | This would be more invasive than Option 1 because it changes multiple surfaces (new rung, potentially new board items, etc.) and requires coordination with existing prove/munit tests. | Branch name hint: run-W1-T3760-<epochMs>-cadence-detector | Pros: Clear separation of concerns; easier to test in isolation; can be extended to other cadences without changing their core logic. | Cons: More moving parts, bigger PR, higher risk of collateral test failure; longer cycle for a fix to reach production. | If you pick Option 1, I’ll implement a minimal integration that: | introduces a new state file path for last-seen cadence timestamps, | adds a read function for that path, | adds a small STALE_WINDOW constant (configurable via plan or env if needed), | plumbs the check into decideMeasurementCadence, returning a refused state when stale, | and includes a lightweight test fixture to validate the stale path. | If you pick Option 2, I’ll draft a scoped plan for adding a new detector rung and its glue code, plus tests to ensure it doesn’t regress existing cadences.
- Chosen (RECOMMENDED, auto): Rationale: The prompt notes a CadenceMarker writer (last-seen) that indicates the cadence writer is not running. If last-seen lags behind, the cadence should fail rather than silently appear healthy. This is the smallest, safest single-change approach that directly encodes the “stale rung” condition into the cadence’s own decision flow, without introducing a separate monitor process.
- Risk: medium (medium-risk signal in the decision text)
- Rollback: revert the PR.
