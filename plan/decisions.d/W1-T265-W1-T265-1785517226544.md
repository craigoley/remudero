## 2026-07-31T17:05:43.512Z — W1-T265 (W1-T265-1785517226544)
- Options: Question: What signal should ensureWorkerKeychain's new identity check compare to detect an Anthropic-account switch? | Option A — Follow the task design literally: use the keychain item's acct attribute. | Option B — Source identity from ~/.claude.json's oauthAccount.accountUuid (fallback emailAddress), the field account-usage.ts already established as the actual per-account discriminator. | Reversibility: Both are easily reversible — the identity source is a single internal comparison behind ensureWorkerKeychain's existing seam; swapping A→B or B→A later touches only worker-home.ts (and the one caller wiring in the value), not the public store-path shape (labelled vs. legacy-unlabelled) or the summary/ledger fields, which are identical either way. No data migration risk since store files are content-addressed by label, not by a persisted schema.
- Chosen (RECOMMENDED, auto): Question: What signal should ensureWorkerKeychain's new identity check compare to detect an Anthropic-account switch?
- Risk: medium (medium-risk signal in the decision text)
- Rollback: revert the PR.
