## 2026-09-19T17:45:32.647Z — W1-T3187 (W1-T3187-1789839676125)
- Options: Approach: introduce a shared renderedIds Set (or equivalent) in the parent component that renders both vocabularies. When mapping over escalations for either vocabulary, skip rendering an item if its id is already present in renderedIds; otherwise render it and mark the id as rendered. | Rationale: avoids duplicating the same escalation across two sections that currently use overlapping data sources (Mailbox needsHuman subset). Keeps both vocabularies visually consistent while preventing two live items from appearing in parallel in both sections. | Impact: minimal data-model changes; localized UI change. Requires minimal changes to the two list render paths and a small utility for id-tracking. Adds test coverage to ensure duplicates are not rendered across sections. | Rationale: the bug manifests in the view (two sections showing the same escalation). Fixing solely the backend data shape risks redefining semantics of the two vocabularies and could reintroduce duplication in other views. A front-end dedup guarantees the user-facing symptom is eliminated regardless of data source alignment. | If it turns out that the two vocabularies are intentionally independent and must show identical items in parallel for some workflow reason, we would need to rework the UI to either (a) clearly delineate duplicates as synchronized actions (e.g., a single source of truth with two mirrored, non-conflicting action UIs) or (b) collapse to a single unified view with cross-view actions. This would be a scope increase beyond the current one-change constraint. | Add a small dedupe utility (e.g., getUniqueEscalations by id) and a renderedIds state in the parent component. | Apply a guard in both vocabulary renderers to skip already-rendered items. | Add tests verifying: (1) an escalation appears once on a page with both vocabularies present; (2) actions in one vocabulary do not reflect duplicates; (3) behavior remains stable when data sources produce identical items in different orders. | Update any relevant story/tests to reflect the non-duplicated rendering.
- Chosen (RECOMMENDED, auto): implement de-duplication at render time in the UI so that each escalation is rendered exactly once per page, across both resolution vocabularies.
- Risk: medium (medium-risk signal in the decision text)
- Rollback: revert the PR.
