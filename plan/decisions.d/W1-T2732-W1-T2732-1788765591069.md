## 2026-09-07T07:25:42.942Z — W1-T2732 (W1-T2732-1788765591069)
- Options: Option A — wire ADVISORY (report-only, not a required check) — | Reversibility: high — promoting advisory→required later is a one-line CI config change; nothing built now needs to be undone. | Option B — relax criterion 5 to add a scoped waiver, then wire REQUIRED | Reversibility: lower — modifies the script W1-T2292 owns and passes today, and changes this task's own acceptance criteria; unwinding later means touching both the script and the task record again. | action: pick between Option A and Option B above (or authorize route (c)) so W1-T2732 can be implemented — the task cannot proceed safely without this, per its own note: block. | task: file the META-GATE task the design section recommends (a check that refuses any tracked scripts/-check|-gate executable with no workflow/npm-script caller) — recorded as a recommendation, not filed here, per the task's own design section (v).
- Chosen (RECOMMENDED, auto): Option A — wire ADVISORY (report-only, not a required check) —
- Risk: medium (medium-risk signal in the decision text)
- Rollback: revert the PR.
