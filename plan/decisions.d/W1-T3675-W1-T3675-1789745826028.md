## 2026-09-18T15:51:39.337Z — W1-T3675 (W1-T3675-1789745826028)
- Options: : implement a path-agnostic grep-proof resolver in the test/evaluation harness. Specifically: | Add a small path resolver that, when a grep proof specifies a file by path, will: | If the exact path exists, use it as today. | If not, attempt to locate a matching file by name (basename) and extension anywhere in the repo (and/or via a lightweight git rename-detection lookup) and use that discovered file as the target for the grep test. | If multiple plausible matches are found, fail the proof with a clear message to disambiguate (prompt the reviewer to update the proof or confirm the intended target). | If no suitable file can be resolved (no exact path and no good rename match), fail the proof with a guidance message to update the PR body to reflect the new path or rationale. | On success, the grep test runs against the resolved path and validates the pattern against the file’s contents as intended. | Rationale: this prevents silent invalidation of proofs when a file is renamed (a common CI hygiene problem), keeps proofs durable across refactors, and shifts the burden from CI to an explicit, reviewable path resolution step. | Reversibility: the change is opt-in via a configuration flag (e.g., enablePathResolutionForProofs in proofEngine config). If disabled, behavior reverts to the current exact-path requirement. This allows for a safe rollback if needed. | REVERSE-OPTION: require reviewers to update grep-proof paths whenever a tracked file is renamed (i.e., enforce an explicit path update in the PR body for any proof referencing that file). If a proof references a renamed file and no update is present, CI fails the proof until the PR body is updated to reflect the new path or an alias is configured in the resolution layer. | Rationale: provides a hard PR-level governance signal to keep proofs in sync with repo history, and makes rename-driven breakages explicit rather than silently tolerated. It shifts the burden to reviewers to maintain proofs during refactors, preserving a stricter provenance discipline. | Reversibility: if rename resolution is enabled, reviewers can opt out by adding an explicit alias mapping in the config (instead of forcing a PR body update). If needed, this can be toggled off to revert to the prior silent-failure model.
- Chosen (RECOMMENDED, auto): : implement a path-agnostic grep-proof resolver in the test/evaluation harness. Specifically:
- Risk: medium (medium-risk signal in the decision text)
- Rollback: revert the PR.
