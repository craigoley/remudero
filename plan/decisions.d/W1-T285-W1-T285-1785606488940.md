## 2026-08-01T17:52:52.861Z — W1-T285 (W1-T285-1785606488940)
- Options: Option A — give accepted a real consumer . Add a place that shows accepted work distinctly (e.g. a renderNeedsMe-adjacent "Accepted" section or a status-aware history list) so an entry accepted by button behaves identically to one accepted by merge, and the operator can see the result of clicking Accept, not just its disappearance. Preserves the existing Accept/Reject affordance and existing acceptance-criteria shape (test/accept-status-consumed.test.ts as filed). Budget: $45 as filed. Reversible — a straight revert removes the new consumer and leaves the button as-is (still non-functional, i.e. back to today's bug). | Option B — delete the Accept button/affordance. Since panel-graph.ts:175 already sets accepted on PR merge with no button involved, let merge-state alone drive the lifecycle and remove the manual Accept action (and the now-pointless "accept" branch of POST /v1/feedback/decision) from both consoles. Cheaper, shrinks scope to a deletion, but changes operator-facing behavior (removes a control) and requires rewriting the acceptance criteria (the filed criteria assume a surviving consumer, not a deletion) — the task file says as much. Reversible via revert, but a UI-affordance removal is more disruptive to re-add later than A is to unwind.
- Chosen (RECOMMENDED, auto): Option A — give accepted a real consumer . Add a place that shows accepted work distinctly (e.g. a renderNeedsMe-adjacent "Accepted" section or a status-aware history list) so an entry accepted by button behaves identically to one accepted by merge, and the operator can see the result of clicking Accept, not just its disappearance. Preserves the existing Accept/Reject affordance and existing acceptance-criteria shape (test/accept-status-consumed.test.ts as filed). Budget: $45 as filed. Reversible — a straight revert removes the new consumer and leaves the button as-is (still non-functional, i.e. back to today's bug).
- Risk: medium (medium-risk signal in the decision text)
- Rollback: revert the PR.
