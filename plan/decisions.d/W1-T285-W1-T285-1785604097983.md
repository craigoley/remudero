## 2026-08-01T17:12:48.530Z — W1-T285 (W1-T285-1785604097983)
- Options: Two writers, confirmed: src/lib/panel-graph.ts:175 (reconcileFeedbackEntries, sets accepted when the proposal's PR merges — no button involved) and :382 (buildProposalDecisionRoute, sets accepted when the operator clicks Accept, data-decision="accept" in serve.ts:1786). | Zero consumers, confirmed: renderNeedsMe (serve.ts:1839-1841) only renders rows for status === "grilling" or status === "proposed". Once a row leaves "proposed" it vanishes from NEEDS ME — but that's true identically for "accepted" and "rejected"; nothing branches on accepted specifically, and no other view (history, "accepted work", anything) filters or renders on status === "accepted" anywhere in src/. | Option A — : give accepted a real consumer. | Reversibility: fully reversible — an added view/route-consumer is deletable in a follow-up commit with no data-model impact; accepted entries keep existing on disk either way. | Option B — delete the Accept affordance. | Reversibility: trivially git-revertible, but it's a live UX removal — an operator relying on manually accepting a still-open proposal loses that control until/unless a new mechanism replaces it.
- Chosen (RECOMMENDED, auto): Option A — : give accepted a real consumer.
- Risk: medium (medium-risk signal in the decision text)
- Rollback: revert the PR.
