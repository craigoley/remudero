[
  {
    "step": "acceptance.repair.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "acceptance.repaired",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "adhoc_lane.reap",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "adhoc_lane.reap.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "adhoc_lane.reap.undecidable",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "adhoc_lane.unmanaged",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "already_satisfied.refused",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "already_satisfied.unverified",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "anchor.built",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "approve.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "approve.id_materialized",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "approve.shards_written",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "auto_triage.already_detached",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "auto_triage.check_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "auto_triage.detached",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "auto_triage.fired",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "auto_triage.run_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "auto_triage.skipped",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "auto_triage.skipped_inflight",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "auto_triage.stood_down",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "automerge.hold_withdrawal",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "board_gateway.fetch_bytes",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "board_gateway.fetch_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "board_gateway.fetch_ok",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "board_gateway.issue_fetch_bytes",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "board_gateway.issue_fetch_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "board_gateway.issue_fetch_ok",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "board_review.check_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "board_review.fired",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "board_review.ran",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "board_review.run_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "board_review.skipped",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "board_snapshot.commit_refused",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "board_snapshot.committed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "board_snapshot.load_refused",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "board_snapshot.loaded",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "board_snapshot.unchanged",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "branch_reap.dry_run",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "branch_reap.pruned",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "budget.warning",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "changeset_claim.repair.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "changeset_claim.repaired",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "ci.polling",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "ci.stalled",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "ci_learning_cadence.check_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "ci_learning_cadence.fired",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "ci_learning_cadence.ran",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "ci_learning_cadence.run_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "ci_learning_cadence.skipped",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "citations.baseline_refused",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "citations.stamp.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "citations.stamped",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "cli.invoked",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "cli.unhandled_rejection",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "console.drain_consumed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "console.kick_dispatched",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "console.kick_refused",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "console.provider_routing_policy_written",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "containment.probe",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "contradiction.resolved",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "contradiction.verdict",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "correction.provenance",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.alive",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.api_window_hold",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.block.awaiting_merge",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.block.fixable_dispatch",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.block.independent_failure",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.block.transient_retry",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.blocked",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.boot",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.claude_bin",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.clone_reap",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.cost_ceiling_reload_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.cost_governor",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.crashloop.escalated",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.crashloop_check",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.detached_action_abandoned",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.disk_headroom.escalated",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.dispatch.wake_deferred",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.escalation.failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.feedback_landing_sweep",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.feedback_landing_sweep.failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.freshness_deferred",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.freshness_drain.completed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.freshness_drain.started",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.governor_check_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.headroom",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.headroom.degraded",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.headroom.park_ceiling.escalated",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.headroom.park_ceiling_forced",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.headroom.unavailable",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.headroom_reserve.escalated",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.headroom_reserve.retired",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.idle",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.idle_reasons",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.inflight_sweep",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.iteration",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.lock_sweep",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.memory_governor",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.orphan_sweep",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.orphan_sweep.failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.paths",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.pause",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.plan_changed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.plan_reload_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.plan_reloaded",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.plan_unchanged",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.provider_routing_policy_fallback",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.provider_routing_status_write_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.queue_governor",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.quiet_hours",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.quota",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.quota_exhausted.escalated",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.quota_exhausted.provenance_discarded",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.retro_subprocess.start",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.retro_subprocess.terminal",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.retro_trigger.already_detached",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.retro_trigger.check_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.retro_trigger.detached",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.retro_trigger.run_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.review_clock.wake_consumed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.scratch_sweep",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.spawn_infra_backoff",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.spawn_infra_blocked",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.start",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.stop",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.summary",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.sweep.abandoned",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.sweep.failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.sweep.retrigger_held",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.sweep.retriggered",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.sweep.skipped_concurrent",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.sweep_light.failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.target",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.tick",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.tmp_sweep",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.usage_reset_unrecognised",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.worker_home_sweep",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.worker_keychain",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.worker_stall",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "daemon.worker_tail_sweep",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "decision.autochoose",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "deploy.abort_dirty_tree",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "deploy.console_kickstart",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "deploy.console_ok",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "deploy.console_unhealthy",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "deploy.discarded_identical",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "deploy.dry_run",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "deploy.idle_ceiling_forced",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "deploy.kickstart",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "deploy.not_idle",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "deploy.ok",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "deploy.pulled",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "deploy.skip",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "deploy.unhealthy_rollback",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "diagnose.done",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "diagnose.spawn",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "diagnose.worker_done",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "digest_cadence.check_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "digest_cadence.fired",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "digest_cadence.ran",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "digest_cadence.run_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "digest_cadence.skipped",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "dispatch.circuit_broken",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "dispatch.circuit_broken.escalated",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "dispatch.claim",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "dispatch.claim_release_error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "dispatch.claim_released",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "dispatch.concurrent_set",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "dispatch.indeterminate",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "dispatch.lane_governed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "dispatch.lifetime_capped",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "dispatch.lifetime_capped.escalated",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "dispatch.live_state_indeterminate",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "dispatch.open_sibling_build",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "dispatch.refused_already_merged",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "dispatch.rerun_override",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "dispatch.serialized",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "dispatch.settled_set",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "dispatch.skipped",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "dispatch.stale_credit_excluded",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "dispatch.starvation.cleared",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "dispatch.starvation.escalated",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "dispatch.stood_down",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "dispatch.wip_deferred",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "drain.blocked",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "drain.continued",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "drain.cost_governor",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "drain.escalation.failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "drain.headroom",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "drain.headroom.degraded",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "drain.headroom.unavailable",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "drain.iteration",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "drain.lane_error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "drain.pause",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "drain.queue_governor",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "drain.quota",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "drain.quota_check.failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "drain.start",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "drain.stop",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "drain.summary",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "escalation.batched_away",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "escalation.deduped",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "escalation.demoted",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "escalation.failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "escalation.issue_opened",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fallback_push.cause",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "feedback.landing_sweep",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "feedback_docket.empty",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "feedback_docket.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.base_refresh_checked",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.base_refresh_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.base_refresh_read_error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.base_refreshed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.body_claim_recognition",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.body_claim_update_error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.body_claim_updated",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.body_fetch_error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.body_gate_check_error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.body_gate_repair_error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.ci_evidence_disagreement",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.ci_failures_fetch_error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.ci_false_block",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.ci_not_green",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.dispatch",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.done",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.exhausted",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.false_block",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.generator_fix",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.generator_fix_escalate",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.instrument_entangled",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.live_head_indeterminate",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.live_state_indeterminate",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.parked",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.plan.unavailable",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.prerequisite_ci_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.prerequisite_dispatch_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.prerequisite_opened",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.rebase_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.rebase_refused",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.rebased",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.resolved",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.resumed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.retrigger",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.review",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.round_commits_read_error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.spawn_abandoned",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.spawn_infra_blocked",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.spawn_reclaim_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.spawn_reclaim_no_match",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.spawn_reclaimed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.spawn_settled_after_abandon",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fix.stood_down",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fleet.pause",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fleet.presence",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fleet.resume",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fleet.stop",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "fleet.stop.noop",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "flight_judge.action",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "flight_judge.diagnose_dispatched",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "flight_judge.escalated",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "flight_judge.verdict",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "followup.deduped",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "followup.harvested",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "followup.retired",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "git.stale_dispatch",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "github.wake.accepted",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "github.wake.boot_pending",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "github.wake.check_settle_callback_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "github.wake.check_settled",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "github.wake.duplicate",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "github.wake.ignored",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "github.wake.refused",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "github.wake.unavailable",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "github.wake.watch_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "github_posture.check_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "github_posture.finding",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "implement.done",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "implement.resumed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "inbox.classified",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "inbox.draft_attempts_reopened",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "inbox.draft_batch",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "inbox.draft_batch.deferred",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "inbox.draft_batch.skipped",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "inbox.draft_deferral_recorded",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "inbox.draft_error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "inbox.draft_relint",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "inbox.draft_rung.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "inbox.draft_synthesized",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "inbox.drafted",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "inbox.polled",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "inbox.registry_healed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "inflight.refused",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "isolation.probe",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "issues.polled",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "learning.refused_tainted",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "learnings.injected",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "ledger.rotation_shed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "lint.blocked",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "lint.warned",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "main.health.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "main.health.escalated",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "main.health.observed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "main.health.resolved",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "measurement_cadence.check_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "measurement_cadence.fired",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "measurement_cadence.ran",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "measurement_cadence.run_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "measurement_cadence.skipped",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "mount.class_fallback",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "mount.recon_unrouted",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "mount_recommendation.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "mount_recommendation.objective_fallback",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "mount_recommendation.swept",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "notify.sent",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "onboard.answered",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "operator_notes.injected",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "ops.alerts_polled",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "orientation.next_task.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "orientation.regenerated",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "orientation.write.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "plan.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "plan.grill_opened",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "plan.id_check",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "plan.id_minted",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "plan.relint_refused",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "plan.start",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "plan.synthesized",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "plan.verdict",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "plan_index.regen.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "plan_index.regenerated",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "pr.checks",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "pr.head_provider",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "pr.merged",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "pr.opened",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "pr.polling",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "pr.stalled",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "pr_create.rate_limited",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "preflight.binary_pin",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "preflight.failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "promotion.promoted",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "promotion.scrub",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "promotion.verdict",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "prompt.linted",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "prompt.manifest",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "ratify.approve_refused",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "ratify.approved",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "ratify.reframed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "recon.absent",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "recon.degraded",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "recon.done",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "recon.invalidated",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "recon.masked",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "recon.retry",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "recon.reused",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "relay.bad_frame",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "relay.bad_url",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "relay.connected",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "relay.disconnected",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "relay.forward_bad_path",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "relay.forward_error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "relay.forward_response_error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "relay.hello_ok",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "relay.hello_reject",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "relay.socket_error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "relay.start",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "relay.stop",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "report.followups",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "retro.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "retro.marker.advanced",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "retro.marker.corrupt",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "retro.no_op",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "retro.pr.recovered",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "retro.preflight_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "retro.preflight_passed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "retro.preflight_repair",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "retro.preflight_repair_stood_down",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "retro.publication.escalated",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "retro.start",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "retro.synthesized",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "retry.classified",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "retry.exhausted",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "retry.success",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "retry.usage_limit",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "review.body_fetch_error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "review.capacity",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "review.decision_conflict_escalated",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "review.downgrade_suppressed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "review.input_body_fetch_error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "review.pending_post.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "review.pending_posted",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "review.post_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "review.post_refused",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "review.posted",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "review.provider_provenance",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "review.resolver_divergence",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "review.reviewer",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "review.reviewer.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "review.reviewer.materialized",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "review.reviewer.skipped",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "review.rubric.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "review.semantic_mode",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "review.unwired_advisory",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "review.verdict_conflict",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "risk_judge.decision",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "risk_judge.escalated",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "run.disk_reclaim",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "run.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "run.start",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "rung.foreign_tree",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "rung.unratified",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "scope_guard.diff_unreadable",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "scope_guard.overrun",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "self_sync.refused",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "serve.bind_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "serve.bind_retry",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "serve.feedback_expander_resolved",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "serve.feedback_expander_unavailable",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "serve.log_mode_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "serve.off_main",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "serve.policy_unreadable",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "serve.stale_code_exit",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "serve.start",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "serve.stop",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "service.access_jwt_error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "service.access_key_refresh_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "service.confirm_nonce_refused",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "service.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "service.forbidden",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "service.forbidden_sensitivity",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "service.forbidden_tier",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "service.sse.close",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "service.sse.open",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "service.unauthorized",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "settings.validated",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "source_size.followup.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "source_size.followup.noop",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "specialist.panel",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.absent_repush",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.action_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.armed_stalled",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.base_caused_release.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.check_requeue.dispatched",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.check_requeue.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.check_requeue.no_job_id",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.ci_gate_reaggregate.dispatched",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.ci_gate_reaggregate.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.ci_gate_reaggregate.no_job_id",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.ci_gate_reaggregate.rollup_error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.ci_gate_rollup.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.close.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.cost_anomaly.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.credit_backfill",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.credit_backfill.summary",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.dispose",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.dispose.indeterminate",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.dispose.not_open",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.disposed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.escalation_close_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.escalation_closed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.escalation_reconcile.summary",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.fix.ceiling_exhausted",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.fix.checkout_claim_declined",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.fix.checkout_owner_reclaimed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.fix.checkout_recovered",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.fix.checkout_refused",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.fix.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.fix.indeterminate",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.fix.not_open",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.fix.synthetic_task",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.headroom_recovery.close_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.headroom_recovery.failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.headroom_recovery.list_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.headroom_recovery.retired",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.headroom_recovery.summary",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.hold_withdrawal_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.pass",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.plan.unavailable",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.plan_filing_classified",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.post_fix_redrive_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.post_fix_redriven",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.post_fix_reverification.summary",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.post_review.attempt",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.post_review.done",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.post_review.failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.post_review.stall_notice_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.post_review.stalled.escalated",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.ratchet_repair_declined",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.ratchet_repaired",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.red_base_refresh.attempted",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.red_base_refresh.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.red_base_refresh.live_indeterminate",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.red_base_refresh.read_error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.repair_filing.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.repair_filing_suppressed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.repeat_escalate_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.repeat_escalated",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.summary",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.update_branch.attempted",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep.update_branch.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "sweep_light.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "trailer_stamp.failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "triage.already_done",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "triage.claim",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "triage.claim_released",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "triage.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "triage.grill_opened",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "triage.id_check",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "triage.id_minted",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "triage.live_scope_check_error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "triage.relint_refused",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "triage.start",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "triage.synthesized",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "usage.probe_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "verdict.merged",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "wipetest.cadence.check_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "wipetest.cadence.fired",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "wipetest.cadence.refused",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "wipetest.cadence.skipped",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "worker.spawn_error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "worktree.add_failed",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "worktree.base_check_degraded",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "worktree.prune",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "worktree.reap.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "worktree.reap.undecidable",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "worktree.reap_boot",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "worktree.reap_boot.undecidable",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "worktree.reaped",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "worktree.remove",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "worktree.remove.error",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "worktree.stale_base",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  },
  {
    "step": "x.y",
    "reason": "Pre-registry literal, bulk-seeded at W1-T2764 filing (this ratchet's introduction) so the baseline exactly matches every step literal already present in src/ at that time -- not yet decoded into src/lib/ledger-steps.ts's LEDGER_STEPS with a real writer + outcome read. Move an entry OUT of this file and INTO the registry to shrink this list; never widen this file for a NEW literal without a distinct, specific reason of its own."
  }
]
