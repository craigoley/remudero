{
  "_comment": "Mutation-testing ratchet floor (W1-T96, MASTER-PLAN §5 TIER 2 gate 2/4). Captured from a real `npx stryker run` against stryker.conf.json's scoped mutate target (src/lib/classify.ts) via the command test runner (`npm test`). SCOPE NOTE: mutation testing with the command runner reruns the whole suite per mutant (no coverage-based test filtering exists for Node's built-in test runner yet), so the mutate glob is deliberately scoped to one pure-logic module today to keep the CI job inside ci-gate.yml's 15-minute required-check wait ceiling -- widening it later is a one-line glob change here plus a baseline recapture, no script or CI changes required. Never lower this number to make a PR pass -- raise it (ratchet up) when the mutation score genuinely improves; a test-suite-weakening PR must go CI-red on scripts/mutation-ratchet.mjs.",
  "capturedAt": "2026-07-18",
  "mutateScope": ["src/lib/classify.ts"],
  "scorePct": 75.92,
  "killed": 70,
  "timeout": 12,
  "survived": 26,
  "noCoverage": 0,
  "nightly": {
    "_comment": "Nightly full-scope floor (W1-T133, .github/workflows/mutation-nightly.yml). A SIBLING of this file's root-level PR-gate fields above -- untouched by them, and they are untouched by it. BOOTSTRAP VALUE: unlike every other baseline number in this repo, this is NOT captured from a real run -- a real full-tree run costs ~4 hours (the reason this task exists), too expensive to execute just to seed this file. scorePct 0 means the first scheduled nightly run always passes; a human must replace this with that run's REAL measured score (scripts/mutation-nightly-scope.json's scope config decides which files it covers) as a deliberate, reviewed follow-up ratchet-up, same as any other floor bump. Never lower it to make a run pass -- raise it only when a real measured nightly score justifies it.",
    "capturedAt": null,
    "scopeConfig": "scripts/mutation-nightly-scope.json",
    "scorePct": 0,
    "killed": 0,
    "timeout": 0,
    "survived": 0,
    "noCoverage": 0
  }
}
