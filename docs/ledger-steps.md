<!--
AUTO-GENERATED -- DO NOT EDIT BY HAND.
Generated by scripts/generate-ledger-steps.mjs from the LEDGER_STEPS registry in
src/lib/ledger-steps.ts -- the SAME source `meaningOfStep` (read by `rmd ledger-grep`)
resolves against (W1-T2764). Run `npm run ledger-steps` to regenerate after changing
LEDGER_STEPS. `npm run ledger-steps:check` fails CI if this file has drifted from a
fresh regeneration (test/every-ledger-step-has-a-meaning.test.ts).
-->

# Ledger step registry

Every ledger step this repo has DECODED, rendered from the same `LEDGER_STEPS` registry `meaningOfStep` reads (W1-T2764) -- this file cannot name a meaning the registry doesn't have, or drift out of sync with what `rmd ledger-grep` actually prints beside a matched row. A step's own literal is NEVER renamed here or anywhere else (DECISIONS.md, 2026-08-15: ledger step names are query keys). A step this page does not list is not undecoded FOREVER -- it is exempted, with a written reason, in `scripts/ledger-steps-baseline.json` until someone reads its writer closely enough to add a row here.

## `automerge`

### `automerge.arm_skipped`

Auto-merge was NOT attempted for this PR. `logArmAttribution` files every ArmOutcome that is neither armed/direct-merged (see `automerge.armed`) nor one of the three genuinely-attempted-and-failed outcomes (see `automerge.arm_failed`, named here but NOT independently registered — it is produced only via `armSkipStepName`'s indirect return, never a literal this registry's static census can see) under this name. `armIfVerdictPermits` also logs it directly, before any arm is even attempted, for a dependabot PR (the dep-review lane owns arming those) or when `decideAutoMergeArm` itself refuses.

- **writer(s):** `logArmAttribution`, `armIfVerdictPermits`
- **outcomes:** `no-task-id`, `head-unavailable`, `ledger-refused`, `direct-merge-updated`, `direct-merge-preflight-refused`, `irreversible-refused`, `hold-refused`, `skipped`

### `automerge.armed`

Auto-merge status was recorded for a PR. DISAGREEING WRITERS (the learning's own defect, still live): `runTask` logs this literal UNCONDITIONALLY right after calling `armAutoMergeAtOpen`, before anything is known about whether the arm stuck — the real result rides only on the row's own `outcome` field, which may be any ArmOutcome or `no-merge-boundary-refused`. `logArmAttribution` logs the SAME literal only when `armOutcomeArmed(outcome)` is true (outcome `armed`, `direct-merged`, or a legacy `void` return treated as armed) — a genuine success signal. Never read this step name alone as "arming succeeded"; always read the row's `outcome` field first.

- **writer(s):** `runTask`, `logArmAttribution`
- **outcomes:** `no-task-id`, `head-unavailable`, `ledger-refused`, `armed`, `direct-merged`, `direct-merge-failed`, `direct-merge-updated`, `direct-merge-preflight-refused`, `direct-merge-update-failed`, `arm-error-ignored`, `irreversible-refused`, `hold-refused`, `no-merge-boundary-refused`

### `automerge.capped_override_granted`

An operator ran `rmd review --override-capped-by <name> --override-capped-reason <reason>` against a resolvable task, granting a ledgered escape hatch for one specific head that a later `resolveAutoMergeArm` read (see `automerge.capped_override_used`) may honour.

- **writer(s):** `reviewCommand`
- **outcomes:** `capped-override-granted`

### `automerge.capped_override_used`

A CAPPED verdict (zero proofs executed) was armed anyway because a ledgered operator override was present for this exact head — `resolveAutoMergeArm` files this row naming who granted it, EXCLUDING a plan-only PR (which never consults an override at all, since its capped status is structural, not a gate an override needs to lift).

- **writer(s):** `resolveAutoMergeArm`
- **outcomes:** `capped-override-used`

### `automerge.clean_status_direct_merge`

GitHub refused `gh pr merge --auto` because the PR was ALREADY in clean/mergeable status (every required check green), so `attemptArm`'s fallback merged it directly over REST instead, and `logArmAttribution` filed this row once that fallback's outcome resolved to `direct-merged`.

- **writer(s):** `logArmAttribution`
- **outcomes:** `direct-merged`

### `automerge.disarmed`

A standing auto-merge arm was WITHDRAWN because a verdict computed after PR-open time refused to arm (W1-T1215/impl-BF) — `disarmAutoMerge` reported the withdrawal actually took effect on GitHub. Follows the OUTCOME, never the attempt: a withdrawal GitHub refused files `automerge.disarm_skipped` instead (not independently registered here — like `automerge.arm_failed`, it is produced only via a ternary, never a literal this registry's static census can see).

- **writer(s):** `runTask`, `withdrawArmIfVerdictRefuses`
- **outcomes:** `withdrawn`

### `automerge.rate_limit_refused`

`gh pr merge --auto` failed on an exhausted GitHub rate-limit bucket (W1-T1235). Filed ALONGSIDE whatever step `automerge.armed`/`automerge.arm_skipped`/`automerge.arm_failed` already recorded for this same attempt — the row exists so the spent budget is discoverable by this ONE step name even when `attemptArm`'s REST fallback goes on to rescue the merge (W1-T1255), which would otherwise leave no trace the quota was hit.

- **writer(s):** `logArmAttribution`
- **outcomes:** `no-task-id`, `head-unavailable`, `ledger-refused`, `armed`, `direct-merged`, `direct-merge-failed`, `direct-merge-updated`, `direct-merge-preflight-refused`, `direct-merge-update-failed`, `arm-error-ignored`, `irreversible-refused`, `hold-refused`

## `panel`

### `panel.risk_override_recorded`

An operator acted on a risk-judge escalation instead of merging by hand in silence, recording their disposition (`merged_by_hand` | `redispatched` | `abandoned`, ledger.ts's RISK_OVERRIDE_DISPOSITIONS) and whether the judge itself was wrong or the risk was knowingly accepted (ledger.ts's RISK_OVERRIDE_REASON_CLASSES). RECORDING ONLY — nothing about this row decides dispatch or merge. A CONTRASTING EXHIBIT to the `automerge.*` rows above: `recordRiskOverride` logs this literal (via the `RISK_OVERRIDE_RECORDED_STEP` constant, not a bare quoted literal at the call site, which is why the ratchet's static census cannot find it) on exactly ONE path — validation succeeded — so this step name genuinely IS unconditional and single-meaning; not every step in this repo is the `automerge.armed` defect.

- **writer(s):** `recordRiskOverride`
- **outcomes:** `recorded`
