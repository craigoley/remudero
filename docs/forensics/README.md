# Forensics

This directory holds the measured forensic detail compacted out of source-file comments. Every
page here is a verbatim archive of comment blocks removed from one source file, kept whole so no
incident, measurement or design argument is lost when the code's own comments are shortened. The
source file keeps a one-line `// Why:` pointer wherever that history mattered, and those pointers
cite the page here.
