# Fixture (W1-T2640): the PRE-AMENDMENT shape -- describes only the monolith, exactly what §2 said
# before this task, before the sharded filing home, the monolith-append refusal, and the
# loadPlan merge/duplicate-id rule were added. Proves the assertion actually distinguishes the two
# wordings rather than merely parsing.

## 2. Plan format & contracts

**Per product**: the design repo holds `MASTER-PLAN.md` (human narrative) and machine state in
plan/tasks.yaml. Status is derived from GitHub, never machine-written.
